# dependencies (restored by `npm install`)
node_modules/

# build output (regenerated by `npm run build`)
dist/

# run exports from using the app — generated data, not source
saved_data/

# OS/editor noise
.DS_Store
Thumbs.db
